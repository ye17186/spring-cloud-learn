myhelper:
  plugins:
    token:
      enabled: true

ribbon:
  ConnectTimeout: 1000 # 连接超时时间
  ReadTimeout: 10000 # 处理超时时间
  MaxAutoRetries: 1 # 当前实例重试次数
  MaxAutoRetriesNextServer: 0 # 其他实例重试次数

logging:
  level:
    root: warn
    com.yclouds: debug